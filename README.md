# Go To-Do API

Простой REST API для управления задачами (To-Do), написанный на Go с использованием PostgreSQL.

## ✨ Возможности
- Создание, чтение, обновление и удаление задач (CRUD)
- Хранение данных в PostgreSQL
- Структура проекта: `model`, `handler`, `storage`
- Unit-тесты для хендлеров

## 🚀 Запуск

1. Установите PostgreSQL и создайте БД `todo`
2. Обновите строку подключения в `main.go`
3. Запустите:
   ```bash
   go run main.go